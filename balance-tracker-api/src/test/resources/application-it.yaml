msa:
  auditsystem:
    transaction:
      limit: 5 # Max Transaction Limit to submit to the Audit System - for testing.
      maxAmountPerBatch: 500 # Max absolute amount per batch in £ - for testing.
    scheduler:
      delay-ms: 15000 # in millisecnds

spring:
  application:
  name: balance-tracker-api
  # H2 DB Properties
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: delta
    password: springwater
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  h2:
    console:
      enabled: true
      path: /h2-console